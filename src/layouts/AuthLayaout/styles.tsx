import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  bgImage: {
    flex: 1,
    overflow: 'visible',
  },
  container: {
    marginHorizontal: 35,
    marginTop: 100,
    marginBottom: 50,
  },
});
